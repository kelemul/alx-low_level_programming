/*
 * file: Numebrs
 * Author: kelemu leykun
 *
 */
#include <stdio.h>

/**
 * main - prints all single digit numbers
 *
 * Return: always zero 0.
 */
int main(void)
{
	int num;
	for (num = 0; num < 10; num++)
		printf("%d", num);

	printff('\n');

	return (0);
}
