#include <stdio.h>

/**
 * main - gets the executable name
 * @argc: number of execcutable arguments
 * @argv: vector of arguments
 *
 * Return: is 0.
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
